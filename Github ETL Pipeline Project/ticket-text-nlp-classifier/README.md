# Ticket Text NLP Classifier

Classify service ticket text into categories (e.g., VPN issue, password reset).
Includes preprocessing, TF-IDF baseline model, evaluation, and saved artefacts.

## Quickstart
1. `pip install -r requirements.txt`
2. `python -m src.train_text_model`
